import React from 'react';
import s from './MapText.module.css'

const MapHeader = () => {
    return (
            <div className={s.header}> We are almost <br/> everywhere </div>
    );
};

export default MapHeader;